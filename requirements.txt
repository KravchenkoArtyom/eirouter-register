# Версии с верхней границей: мажорное обновление не должно ломать программу
# на чужой машине без предупреждения.
fastapi>=0.110,<1.0
httpx>=0.27,<1.0
playwright>=1.44,<2.0
pydantic>=2.0,<3.0
uvicorn>=0.29,<1.0

# Нужен только для кнопки «Проверить» у socks5-прокси в панели; в браузере
# socks5 работает и без него. Раскомментируйте, если проверяете socks5:
# httpx[socks]>=0.27,<1.0
